body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    color: #fff;
    background-color: #111;
}

header {
    background-color: #111;
    padding: 1rem;
}

header nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
}

header nav img {
    max-width: 150px;
}

main {
    padding: 2rem;
}

.hero {
    text-align: center;
}

.hero h1 {
    font-size: 3rem;
    font-weight: bold;
}

.hero h2 {
    font-size: 1.5rem;
    margin-top: 10px;
}

.input-container {
    display: flex;
    max-width: 400px;
    margin: 20px auto;
}

input {
    flex: 1;
    padding: 10px;
    border: none;
    border-radius: 5px 0 0 5px;
}

button {
    background-color: red;
    color: #fff;
    padding: 10px 20px;
    border: none;
    border-radius: 0 5px 5px 0;
    cursor: pointer;
}

section {
    margin-bottom: 30px;
}

section h2 {
    font-size: 2rem;
    margin-bottom: 15px;
}

/* Styling for content items (images, titles, etc.) can be added as needed */
